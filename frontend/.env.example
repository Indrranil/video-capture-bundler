# Port the FastAPI backend is listening on locally — used only by `npm run dev`'s proxy.
# Production builds are served same-origin by the backend itself; this has no effect there.
VITE_API_PORT=5000
